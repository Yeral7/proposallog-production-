<!--
  @branch feature/schedulesprototype
  This file was replaced by the requirements doc one folder up.
  Migrations themselves will be written as numbered .sql files in this folder
  once the DB design is agreed.
-->

# Moved

The Labor Log database plan has moved to a **requirements doc** (not migration SQL):

➡ **`../feature-schedulesprototype-requirements.md`**

That file describes how the v1 prototype uses data so we can design the schema together with the DB engineer. Once the model is agreed, actual migration SQL will live here as numbered files (`013_*.sql`, `014_*.sql`, …).
